# Core Framework
# # 1) Create venv (if you don’t already have one)
# python3 -m venv .venv

# # 2) Activate it
# source .venv/bin/activate

# # (optional but recommended) upgrade pip
# pip install --upgrade pip

# # 3) Install Cerberus deps
# pip install -r cerberus/requirements.txt

# # 4) Install Knowledge Base MCP deps (into the same venv)
# pip install -r mcps/knowledge-base/requirements.txt

fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0

# Database
sqlalchemy==2.0.25
asyncpg==0.29.0
alembic==1.13.1

# Redis
redis==5.0.1

# Authentication & Security
python-jose[cryptography]==3.3.0
bcrypt==4.2.0
python-multipart==0.0.6

# HTTP Client
httpx==0.26.0

# Validation & Utilities
email-validator==2.1.0
python-dateutil==2.8.2
python-slugify==8.0.4

# Logging & Monitoring
structlog==24.1.0
python-json-logger==2.0.7

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
httpx==0.26.0

# Development
black==24.1.0
isort==5.13.2
mypy==1.8.0
ruff==0.1.14

# Type stubs
types-python-dateutil==2.8.19.20240106
types-redis==4.6.0.20240106

